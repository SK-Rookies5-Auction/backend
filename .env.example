DB_URL=jdbc:mariadb://localhost:3306/mactadb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
DB_USERNAME=root
DB_PASSWORD=
JWT_SECRET=change-me

# Use "redis" when running the app with docker compose.
# Use "localhost" when running the Spring Boot app directly on your machine.
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change-me
